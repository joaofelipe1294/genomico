<%= render 'layouts/user_navbar' %>

<div class="card col-8 offset-2 mt-5 p-0 mb-5">
  <div class="card-header">
    <h4 class="text-center">
      Editar reagente
    </h4>
  </div>
  <div class="card-body">
    <%= render 'form', reagent: @reagent %>
  </div>
</div>
