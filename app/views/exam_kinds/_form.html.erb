<%= form_for(@exam_kind) do |f| %>
  <% if @exam_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_kind.errors.count, "error") %> prohibited this exam_kind from being saved:</h2>

      <ul>
      <% @exam_kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='field form-group'>
    <%= f.label :name %>
    <%= f.text_field :name, {class: 'form-control'} %>
  </div>
  <div class="field form-group">
    <%= f.label :field_id %>
    <%= f.select :field_id, options_from_collection_for_select(@fields, 'id', 'name'), {}, {class: 'form-control'} %>
  </div>

<!--   <div class="field">
    <%= f.label :is_active %><br>
    <%= f.check_box :is_active %>
  </div> -->

  <button class="btn btn-outline-primary float-right">
    Salvar
  </button>
<% end %>
