<%= render 'layouts/user_navbar' %>

<div class="card mt-5 col-8 p-0 offset-2">
  <div class="card-header bg-info">
    <h4 class="text-center text-white">
      Novo paciente
    </h4>
  </div>
  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>
