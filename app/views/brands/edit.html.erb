<%= render 'layouts/user_navbar' %>

<div class="card mt-5 col-6 offset-3 p-0">
  <div class="card-header bg-secondary">
    <h3 class="text-center text-white">
      Editar marca
    </h3>
  </div>
  <div class="card-body">
    <%= render 'form', brand: @brand %>
  </div>
</div>
